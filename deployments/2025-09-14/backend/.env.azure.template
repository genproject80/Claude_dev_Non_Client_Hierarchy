# Azure Production Environment Variables Template
# These should be set in Azure App Service Application Settings

# Database Configuration - Azure SQL Database
DB_SERVER=genvolt.database.windows.net
DB_DATABASE=gendb
DB_USERNAME=genadmin
DB_PASSWORD=genvolt@123
DB_DRIVER=ODBC Driver 17 for SQL Server

# JWT Configuration (Generate secure secret for production)
JWT_SECRET=your-secure-jwt-secret-32-characters-minimum
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=8080
NODE_ENV=production

# CORS Configuration
FRONTEND_URL=https://polite-smoke-0f45f3a00.1.azurestaticapps.net

# Config Functions Integration
CONFIG_API_URL=https://func-iot-config-prod.azurewebsites.net/api/v1
CONFIG_API_ADMIN_KEY=your-admin-api-key-from-database

# Multiple CORS Origins
CORS_ORIGINS=https://polite-smoke-0f45f3a00.1.azurestaticapps.net,https://func-iot-config-prod.azurewebsites.net

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=1000

# SSL and Security
HTTPS_ONLY=true
TRUST_PROXY=true